# Motion Design Spec Writer — Writes motion design specifications for animation handoff
id: motion-design-spec-writer
name: Motion Design Spec Writer
version: 1.0.0
description: Writes motion design specifications for animation handoff
threshold: 85
max_iterations: 5
model:
  provider: google
  model: gemini-2.5-flash
  max_tokens: 512
  temperature: 0.2
evaluation:
  self_eval_weight: 0.3
  judge_eval_weight: 0.7
  benchmark_cases: []
