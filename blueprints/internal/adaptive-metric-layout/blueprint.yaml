# Adaptive Metric Layout — Creates adaptive metric layouts that respond to data density
id: adaptive-metric-layout
name: Adaptive Metric Layout
version: 1.0.0
description: Creates adaptive metric layouts that respond to data density
threshold: 85
max_iterations: 5
model:
  provider: google
  model: gemini-2.5-flash
  max_tokens: 512
  temperature: 0.2
evaluation:
  self_eval_weight: 0.3
  judge_eval_weight: 0.7
  benchmark_cases: []
