# Api Gateway Designer — Designs API gateway architectures with routing and rate limiting
id: api-gateway-designer
name: Api Gateway Designer
version: 1.0.0
description: Designs API gateway architectures with routing and rate limiting
threshold: 85
max_iterations: 5
model:
  provider: google
  model: gemini-2.5-flash
  max_tokens: 512
  temperature: 0.2
evaluation:
  self_eval_weight: 0.3
  judge_eval_weight: 0.7
  benchmark_cases: []
