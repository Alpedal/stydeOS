# Dashboard Data Pipeline
id: "dashboard-data"
name: "Dashboard Data Pipeline"
version: "0.1.0"
description: "Läser Forge-data (forge.json, runs/) och bygger statistik och aggregat för dashboard-API:et"

threshold: 85
max_iterations: 5

model:
  provider: "openai"
  model: "gpt-4o"

evaluation:
  self_eval_weight: 0.3
  judge_eval_weight: 0.7
  benchmark_cases: []
