# Frontend Component Builder — Builds reusable frontend components with accessibility built-in
id: frontend-component-builder
name: Frontend Component Builder
version: 1.0.0
description: Builds reusable frontend components with accessibility built-in
threshold: 85
max_iterations: 5
model:
  provider: google
  model: gemini-2.5-flash
  max_tokens: 512
  temperature: 0.2
evaluation:
  self_eval_weight: 0.3
  judge_eval_weight: 0.7
  benchmark_cases: []
